import { useEffect, useState } from "react";
import { getCurrentUser } from "../../services/financial.service";
import type { User } from "../../types/financial";
export default function SettingsManager({ token }: { token: string }) { const [user, setUser] = useState<User | null>(null); useEffect(() => { void getCurrentUser(token).then(setUser); }, [token]); function logout() { localStorage.removeItem("financial_ai_access_token"); window.location.assign("/"); } return <main className="min-h-screen bg-slate-950 p-8 text-white"><button onClick={() => window.location.assign("/")} className="text-cyan-400">Dashboard</button><h1 className="mt-5 text-3xl font-bold">Configuración</h1><div className="mt-8 max-w-xl rounded-lg bg-slate-900 p-6"><p className="text-slate-400">Nombre</p><p className="text-xl">{user?.name ?? "Cargando..."}</p><p className="mt-5 text-slate-400">Correo</p><p className="text-xl">{user?.email ?? ""}</p><button onClick={logout} className="mt-8 rounded bg-red-500 px-5 py-3 font-semibold text-white">Cerrar sesión</button></div></main>; }
