import { useEffect, useState } from "react";
import type { FormEvent } from "react";
import { createLiability, deleteLiability, getLiabilities, updateLiability } from "../../services/financial.service";
import type { Liability } from "../../types/financial";
const options = ["Tarjeta de crédito", "Tarjeta de tienda", "Préstamo personal", "Préstamo familiar", "Crédito hipotecario", "Crédito vehicular", "Préstamo estudiantil", "Deuda de negocio", "Impuestos pendientes", "Servicios pendientes", "Otras deudas", "Otra categoría"];
export default function LiabilityManagerV5({ token }: { token: string }) { const [items, setItems] = useState<Liability[]>([]); const [category, setCategory] = useState(options[0]); const [custom, setCustom] = useState(""); const [balance, setBalance] = useState(""); const load = () => getLiabilities(token).then(setItems); useEffect(() => { void load(); }, [token]); async function submit(e: FormEvent<HTMLFormElement>) { e.preventDefault(); const selected = category === "Otra categoría" ? custom.trim() : category; if (!selected) return; await createLiability(token, { name: selected, category: selected, balance: Number(balance) }); setBalance(""); setCustom(""); await load(); } async function edit(item: Liability) { const nextCategory = window.prompt("Categoría", item.category); const nextBalance = window.prompt("Nuevo saldo", String(item.balance)); if (!nextCategory || nextBalance === null || Number(nextBalance) <= 0 || Number.isNaN(Number(nextBalance))) return; await updateLiability(token, item.id, { name: nextCategory, category: nextCategory, balance: Number(nextBalance) }); await load(); } return <main className="min-h-screen bg-slate-950 p-8 text-white"><button onClick={() => window.history.back()} className="text-cyan-400">Volver</button><h1 className="mt-5 text-3xl font-bold">Deudas</h1><form onSubmit={submit} className="mt-6 flex max-w-xl gap-3"><select className="flex-1 rounded bg-slate-800 p-3" value={category} onChange={(e) => setCategory(e.target.value)}>{options.map((x) => <option key={x}>{x}</option>)}</select>{category === "Otra categoría" && <input className="w-40 rounded bg-slate-800 p-3" placeholder="Escribe categoría" value={custom} onChange={(e) => setCustom(e.target.value)} required />}<input className="w-32 rounded bg-slate-800 p-3" type="number" min="0.01" step="0.01" placeholder="Saldo" value={balance} onChange={(e) => setBalance(e.target.value)} required /><button className="rounded bg-cyan-500 px-5 font-semibold text-slate-950">Agregar</button></form><div className="mt-8 max-w-xl space-y-3">{items.map((item) => <div key={item.id} className="flex justify-between rounded-lg bg-slate-900 p-4"><span>{item.category}</span><div className="flex gap-4"><span className="text-red-400">S/ {item.balance.toFixed(2)}</span><button onClick={() => void edit(item)} className="text-cyan-400">Editar</button><button onClick={() => void deleteLiability(token, item.id).then(load)} className="text-red-400">Eliminar</button></div></div>)}</div></main>; }
